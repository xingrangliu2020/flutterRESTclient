name: example
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rest_api_client:
    path: ../
  json_rest_api_client: ^0.1.5
  http: ^ 0.13.4

dev_dependencies:

flutter:
  uses-material-design: true